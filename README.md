# Crop Pest and Disease Detection (Maize & Cashew)
This project uses deep learning to detect pests and diseases in maize and cashew using image data.

## Dataset
We use the raw maize and cashew images as the primary dataset source.
The raw data is split into train/validation/test sets first.
Data augmentation is applied only to the training set to avoid leakage.
